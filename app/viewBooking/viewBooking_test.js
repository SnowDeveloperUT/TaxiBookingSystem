'use strict';

describe('taxiApp.viewBooking module', function () {

	beforeEach(module('taxiApp.viewBooking'));

	describe('viewBooking controller', function () {

		it('should ....', inject(function ($controller) {
			//spec body
			var viewBookingCtrl = $controller('ViewBookingCtrl');
			expect(viewBookingCtrl).toBeDefined();
		}));

	});
});